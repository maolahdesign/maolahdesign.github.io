### Excel 函數與 AI 智能報表應用 測驗題

**說明：** 本測驗共 20 題選擇題，每題有四個選項，請選出最符合題意的答案。

---

**1. 關於 Excel 函數的基本概念，下列敘述何者錯誤？**
A. 函數是一個預先設定好的「捷徑」或「工具」。
B. 函數可以幫助使用者完成特定的計算或操作。
C. 函數的語法結構通常以等號開頭，後接函數名稱和參數。
D. 函數只能處理數值資料，無法處理文字或日期。

**2. 在 Excel 中，若要計算 A1 到 A100 儲存格的數值總和，最有效率的函數是？**
A. `MAX`
B. `SUM`
C. `TODAY`
D. `COUNT`

**3. 關於 Excel 中的引用類型，下列敘述何者正確？**
A. 相對引用在複製公式時，參照的行列會固定不變。
B. 絕對引用在複製公式時，參照的行列會隨著位置改變。
C. 混合引用可以同時鎖定欄和行。
D. 絕對引用使用 `$` 符號來固定參照的行列。

**4. 當 Excel 公式中出現 `#DIV/0!` 錯誤訊息時，通常表示什麼？**
A. 數值或資料型態錯誤。
B. 沒有可用值或找不到匹配。
C. 除數為零。
D. 儲存格為空白。

**5. `SUMIF` 函數的主要用途是？**
A. 計算指定範圍內所有數值的總和。
B. 根據多個條件計算指定範圍內數值的總和。
C. 根據單一條件計算指定範圍內數值的總和。
D. 計算指定範圍內非空白儲存格的數量。

**6. 若要計算所有「蘋果」的總銷售額，且產品名稱在 A2:A9，銷售額在 C2:C9，正確的 `SUMIF` 公式是？**
A. `=SUMIF(A2:A9, "蘋果")`
B. `=SUMIF(A2:A9, "蘋果", C2:C9)`
C. `=SUMIF(C2:C9, "蘋果", A2:A9)`
D. `=SUMIF(A2:A9, C2:C9, "蘋果")`

**7. 在 `SUMIF` 函數中，若條件為「大於 150」，應如何表示？**
A. `150`
B. `>150`
C. `">150"`
D. `=">150"`

**8. `SUMIFS` 函數與 `SUMIF` 函數最主要的區別是？**
A. `SUMIFS` 只能處理文字條件，`SUMIF` 只能處理數字條件。
B. `SUMIFS` 可以根據多個條件加總，`SUMIF` 只能根據單一條件。
C. `SUMIFS` 的語法比 `SUMIF` 更簡單。
D. `SUMIFS` 不支援萬用字元，`SUMIF` 支援。

**9. 若要計算「北區」所有「非蘋果」產品的總銷售額，正確的 `SUMIFS` 公式中，產品條件應如何表示？**
A. `"蘋果"`
B. `"<蘋果>"`
C. `"<>"蘋果`
D. `"<>"&"蘋果"`

**10. `COUNT` 函數的主要用途是？**
A. 計算範圍中包含「數值」的儲存格數量。
B. 計算範圍中包含「非空白」的儲存格數量。
C. 計算範圍中符合「單一條件」的儲存格數量。
D. 計算範圍中符合「多個條件」的儲存格數量。

**11. `NETWORKDAYS` 函數的用途是？**
A. 計算兩個日期之間的天數。
B. 計算兩個日期之間的工作日天數。
C. 計算兩個日期之間的週末天數。
D. 計算兩個日期之間的假日天數。

**12. 在 `NETWORKDAYS.INTL` 函數中，若要設定週末為「週日和週一」，第三個參數應填寫什麼數字代碼？**
A. 1
B. 2
C. 3
D. 7

**13. `IF` 函數的語法結構是？**
A. `IF(值if_true, 條件, 值if_false)`
B. `IF(條件, 值if_true, 值if_false)`
C. `IF(條件, 值if_false, 值if_true)`
D. `IF(值if_false, 條件, 值if_true)`

**14. `IFS` 函數的主要優點是？**
A. 只能處理單一條件判斷。
B. 可以處理多重條件判斷，避免多層 `IF` 巢狀結構。
C. 只能處理數字條件。
D. 只能處理文字條件。

**15. `CONCAT` 函數的主要用途是？**
A. 合併多個範圍或字串中的文字，並自動加入分隔符號。
B. 合併多個範圍或字串中的文字，但不自動加入分隔符號。
C. 計算文字字串的字元個數。
D. 從文字字串的開頭傳回指定數量的字元。

**16. 若要從文字字串 "SKU-2025-XYZ" 中擷取 "2025"，應使用哪個函數？**
A. `LEFT`
B. `RIGHT`
C. `MID`
D. `LEN`

**17. `TEXTJOIN` 函數相較於 `CONCAT` 函數，最主要的優點是？**
A. 只能合併兩個字串。
B. 可以指定分隔符號，並選擇是否忽略空白儲存格。
C. 執行速度更快。
D. 支援更多資料類型。

**18. 在 Excel 中，`FILTER` 函數的主要用途是？**
A. 對資料範圍進行排序。
B. 將多個範圍或陣列水平堆疊在一起。
C. 根據指定條件，從資料範圍中篩選出符合條件的紀錄。
D. 從指定範圍或陣列中，傳回一組不重複的唯一值清單。

**19. 關於 AI 在數據分析中的角色，下列敘述何者錯誤？**
A. AI 可以作為「探索者」，幫助提出值得分析的商業問題。
B. AI 可以作為「翻譯官」，將人類語言轉換為 Excel 函數公式。
C. AI 可以作為「解讀者」，幫助解讀數據趨勢與洞察。
D. AI 將完全取代人類分析師，自主完成所有數據分析工作。

**20. 一個好的 Prompt (提示詞) 應具備哪些要素？**
A. 清晰明確、提供足夠情境、明確指令、指定輸出格式。
B. 越模糊越好，讓 AI 有更多發揮空間。
C. 只需要提供目標，不需要提供任何情境。
D. 僅限於單一詞彙或短語。

---

**參考答案：**

1.  D
2.  B
3.  D
4.  C
5.  C
6.  B
7.  C
8.  B
9.  C
10. A
11. B
12. B
13. B
14. B
15. B
16. C
17. B
18. C
19. D
20. A

---